(()=>{const D=window.Dopa;let mode='focus',seconds=D.state.settings.focus*60,total=seconds,tick=null,running=false;const html=()=>{const st=D.state.sessions.filter(x=>x.date===D.today()),t=D.activeTask();return `<div class="focus-widget"><div class="timer-stage"><div class="timer-ring" style="--progress:${((total-seconds)/total)*100}"><div class="timer-center"><small>${mode==='focus'?'◷ Focus Time':'Break'}</small><strong data-time>${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}</strong><span>${running?'Stay with it':'Ready to focus'}</span></div></div></div><div class="timer-controls"><div class="segmented"><button data-mode="focus" class="${mode==='focus'?'active':''}">Pomodoro</button><button data-mode="short" class="${mode==='short'?'active':''}">Short Break</button><button data-mode="long" class="${mode==='long'?'active':''}">Long Break</button></div><div class="focus-task-card"><small>Current task</small><strong>${t?D.esc(t.title):'No active task'}</strong></div><div class="timer-buttons"><button class="secondary" data-reset>↺</button><button class="primary" data-toggle>${running?'Pause':'Start Focus'}</button></div></div><div class="timer-side"><div class="timer-stat"><span>Today's focus</span><strong>${D.mins(st.reduce((a,x)=>a+x.minutes,0))}</strong></div><div class="timer-stat"><span>Sessions</span><strong>${st.length}</strong></div></div></div>`};function setMode(m){clearInterval(tick);running=false;mode=m;const n=m==='focus'?D.state.settings.focus:m==='short'?D.state.settings.short:D.state.settings.long;seconds=total=n*60;D.renderTimer()}function toggle(){running=!running;clearInterval(tick);if(running)tick=setInterval(()=>{seconds--;D.$$('[data-time]').forEach(e=>e.textContent=`${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`);D.$$('.timer-ring').forEach(e=>e.style.setProperty('--progress',((total-seconds)/total)*100));if(seconds<=0)finish()},1000);D.renderTimer()}function finish(){clearInterval(tick);running=false;if(mode==='focus'){const m=Math.round(total/60);D.state.sessions.push({id:D.uid('sess'),date:D.today(),time:new Date().toISOString(),minutes:m,taskId:D.state.activeTaskId});D.state.journal.unshift({id:D.uid('log'),time:new Date().toISOString(),title:`Focused ${m} min`,note:D.activeTask()?.title||''});D.state.streak=Math.max(1,D.state.streak||0);D.save();D.toast('Focus complete')}setMode(mode==='focus'?'short':'focus');D.renderAll()};D.renderTimer=()=>['dashboardTimer','focusTimer'].forEach(x=>{const e=D.$(x);e.innerHTML=html();e.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));e.querySelector('[data-toggle]').onclick=toggle;e.querySelector('[data-reset]').onclick=()=>setMode(mode)});D.resetTimerSettings=()=>setMode(mode);})();
